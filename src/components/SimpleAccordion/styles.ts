import styled from 'styled-components'

export const Container = styled.div`
  margin: auto;
  justify-content: center;
  width: 90%;

  h1 {
    color: rgb(0, 0, 0);
    font-size: 40px;
    text-align: center;
  }
`
